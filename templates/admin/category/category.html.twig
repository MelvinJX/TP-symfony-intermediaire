{% extends "bases_admin.html.twig" %}

{% block main %}
    <main>
        <h2>Categorie(admin)</h2>
        <section>
            <article>
                <h3>{{ category.name }}</h3>
                <h3>{{ category.description }}</h3>
            </article>
        </section>
    </main>
{% endblock %}